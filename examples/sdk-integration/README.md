# SDK Integration Example

This example demonstrates how external platforms (like CI systems or agent harnesses) can programmatically invoke the OWIS SDK to parse and validate workspaces.

## Running the Example

```bash
node index.js
```
