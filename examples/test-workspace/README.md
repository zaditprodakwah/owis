# Test Workspace

This is an example test workspace used to verify the functionality of the OWIS Reference Runtime engine.

It contains an `owis.json` configuration file conforming to the workspace schema guidelines.
