# API Documentation

## GET /api/items

Returns a list of all items.

**Response:** `200 OK` with JSON array.

## POST /api/items

Creates a new item.

**Request Body:** JSON object with item fields.

**Response:** `201 Created` with the created item.
