# Architecture

This full-stack app follows a three-tier architecture:

1. **Frontend** — React SPA served via CDN
2. **Backend** — Node.js REST API
3. **Shared** — Common utilities used by both tiers
